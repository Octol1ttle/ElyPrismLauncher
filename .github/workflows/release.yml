name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "OasisLauncher-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/OasisLauncher-source OasisLauncher-${{ env.VERSION }}
          mv OasisLauncher-Linux-Qt6-Portable*/OasisLauncher-portable.tar.gz OasisLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv OasisLauncher-Linux-aarch64-Qt6-Portable*/OasisLauncher-portable.tar.gz OasisLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv OasisLauncher-*.AppImage/OasisLauncher-*-x86_64.AppImage OasisLauncher-Linux-x86_64.AppImage
          mv OasisLauncher-*.AppImage.zsync/OasisLauncher-*-x86_64.AppImage.zsync OasisLauncher-Linux-x86_64.AppImage.zsync
          mv OasisLauncher-*.AppImage/OasisLauncher-*-aarch64.AppImage OasisLauncher-Linux-aarch64.AppImage
          mv OasisLauncher-*.AppImage.zsync/OasisLauncher-*-aarch64.AppImage.zsync OasisLauncher-Linux-aarch64.AppImage.zsync
          mv OasisLauncher-macOS*/OasisLauncher.zip OasisLauncher-macOS-${{ env.VERSION }}.zip
          mv OasisLauncher-macOS*/OasisLauncher.dmg OasisLauncher-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf OasisLauncher-${{ env.VERSION }}.tar.gz OasisLauncher-${{ env.VERSION }}

          for d in OasisLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="OasisLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv OasisLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in OasisLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="OasisLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv OasisLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in OasisLauncher-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="OasisLauncher-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv OasisLauncher-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Oasis Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            OasisLauncher-Linux-x86_64.AppImage
            OasisLauncher-Linux-x86_64.AppImage.zsync
            OasisLauncher-Linux-aarch64.AppImage
            OasisLauncher-Linux-aarch64.AppImage.zsync
            OasisLauncher-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            OasisLauncher-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            OasisLauncher-Windows-MinGW-w64-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            OasisLauncher-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            OasisLauncher-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            OasisLauncher-Windows-MSVC-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            OasisLauncher-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            OasisLauncher-macOS-${{ env.VERSION }}.zip
            OasisLauncher-macOS-${{ env.VERSION }}.dmg
            OasisLauncher-${{ env.VERSION }}.tar.gz
